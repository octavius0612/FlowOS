name: Build iOS IPA (V27 - Stable Architecture)

on:
  workflow_dispatch:

env:
  # On désactive la nouvelle architecture instable
  RCT_NEW_ARCH_ENABLED: 0
  NO_FLIPPER: 1

jobs:
  build:
    runs-on: macos-15

    steps:
      - name: 🏗 Setup Repo
        uses: actions/checkout@v3

      - name: 🏗 Setup Node 20
        uses: actions/setup-node@v3
        with:
          node-version: 20
          cache: 'npm'

      - name: 🛡 Force Configs
        run: |
          echo "module.exports = function(api) { api.cache(true); return { presets: ['babel-preset-expo'], plugins: ['react-native-reanimated/plugin'], }; };" > babel.config.js
          echo "const { getDefaultConfig } = require('expo/metro-config'); const config = getDefaultConfig(__dirname); module.exports = config;" > metro.config.js
          rm -rf node_modules ios android .expo
          # On change l'ID pour éviter les conflits de cache
          sed -i '' 's/"bundleIdentifier": ".*"/"bundleIdentifier": "com.flowos.stable.v27"/' app.json || echo "app.json missing"

      - name: 📦 Install STABLE Dependencies (Expo 51)
        run: |
          npm install -g expo-cli
          
          # On force le couple Expo 51 / RN 0.74 qui est ultra stable
          npm install \
            expo@^51.0.0 \
            react-native@0.74.5 \
            react-native-reanimated@3.10.1 \
            react-native-gesture-handler@2.16.1 \
            react-native-safe-area-context@4.10.5 \
            react-native-screens@3.31.1 \
            expo-status-bar@~1.12.1 \
            expo-linear-gradient@~13.0.2 \
            expo-asset@~10.0.10 \
            expo-font@~12.0.9 \
            expo-constants@~16.0.2 \
            expo-file-system@~17.0.1 \
            @expo/vector-icons@^14.0.0 \
            @google/generative-ai \
            @react-native-async-storage/async-storage

          # Outils de build compatibles
          npm install --save-dev \
            @react-native-community/cli \
            @react-native-community/cli-platform-ios \
            metro-config \
            @react-native/metro-config \
            @expo/metro-config \
            babel-preset-expo \
            babel-plugin-module-resolver

      - name: 🚀 Prebuild
        run: npx expo prebuild --platform ios --clean

      - name: 💉 Disable Signing (Force)
        run: |
          cd ios
          # Injection Podfile
          sed -i '' '/post_install do |installer|/a\
            installer.pods_project.targets.each do |target|\
              target.build_configurations.each do |config|\
                config.build_settings["CODE_SIGNING_ALLOWED"] = "NO"\
                config.build_settings["CODE_SIGNING_REQUIRED"] = "NO"\
                config.build_settings["CODE_SIGNING_IDENTITY"] = ""\
                config.build_settings["EXPANDED_CODE_SIGN_IDENTITY"] = ""\
              end\
            end\
          ' Podfile
          
          # Nettoyage Projet
          find . -name "project.pbxproj" -print0 | xargs -0 sed -i '' 's/CODE_SIGNING_ALLOWED = YES/CODE_SIGNING_ALLOWED = NO/g'
          find . -name "project.pbxproj" -print0 | xargs -0 sed -i '' 's/CODE_SIGNING_REQUIRED = YES/CODE_SIGNING_REQUIRED = NO/g'
          find . -name "project.pbxproj" -print0 | xargs -0 sed -i '' 's/"CODE_SIGN_IDENTITY\[sdk=iphoneos\*\]" = "iPhone Developer";/"CODE_SIGN_IDENTITY[sdk=iphoneos*]" = "";/g'

      - name: 🥥 Install Pods
        run: |
          cd ios
          # On s'assure que la New Arch est bien désactivée
          RCT_NEW_ARCH_ENABLED=0 pod install --repo-update

      - name: 📦 Manual Bundle JS
        run: |
          mkdir -p ios/assets
          npx react-native bundle \
            --entry-file index.js \
            --platform ios \
            --dev false \
            --bundle-output ios/main.jsbundle \
            --assets-dest ios \
            --reset-cache

      - name: 🔨 Build Native App
        id: build_step
        run: |
          cd ios
          export SKIP_BUNDLING=1
          set -o pipefail
          xcodebuild \
            -workspace FlowOS.xcworkspace \
            -scheme FlowOS \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath build \
            -destination 'generic/platform=iOS' \
            clean build \
            CODE_SIGN_ENTITLEMENTS="" \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_IDENTITY="" \
            RCT_NEW_ARCH_ENABLED=0 \
            2>&1 | tee raw_build.log

      - name: 📦 Package IPA
        if: success()
        run: |
          cd ios
          mkdir Payload
          APP_PATH=$(find build/Build/Products/Release-iphoneos -name "*.app" | head -n 1)
          if [ -z "$APP_PATH" ]; then
            echo "CRITICAL FAILURE: .app not found."
            exit 1
          fi
          cp -R "$APP_PATH" Payload/
          zip -r FlowOS.ipa Payload

      - name: 📤 Upload IPA
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: flowos-ipa
          path: ios/FlowOS.ipa

      - name: 🔍 Debug Report
        if: failure()
        run: |
          echo "=== RAPPORT V27 ===" > crash_report.txt
          tail -n 100 ios/raw_build.log >> crash_report.txt || echo "Pas de log" >> crash_report.txt

      - name: 📤 Upload Report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ERROR-REPORT
          path: crash_report.txt
